# node / next
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# rust / soroban
target/
# Cargo.lock IS committed for the contract — pins a working dep set (see contracts/poll)

# env & secrets
.env
.env.local
.stellar/

# os
.DS_Store
